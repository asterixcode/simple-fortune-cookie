name: Release Pipeline (Production)
on:
  push:
    branches:
      - test/*
      - release/**
steps:
  k8s:
    runs-on: self-hosted
    needs:
      - upload-to-dockerhub
    steps:
      - name: k8s deployment
        run: chmod +x ci/k8s.sh && ci/k8s.sh

